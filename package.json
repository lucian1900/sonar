{
  "name": "sonar",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel serve src/index.html",
    "serve": "ws --directory dist/",
    "build": "tsc",
    "postbuild": "cp -r static/ dist/ && cp package.json dist/package.json && cd dist && npm install --only=production",
    "publish": "gh-pages -d dist"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "howler": "^2.2.0"
  },
  "devDependencies": {
    "@types/howler": "^2.2.1",
    "gh-pages": "^3.1.0",
    "local-web-server": "^4.2.1",
    "parcel": "^1.12.4",
    "ts-loader": "^8.0.2",
    "typescript": "^3.9.7"
  }
}
